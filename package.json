{
  "name": "dreclaw",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "auth:import": "node scripts/auth-import.mjs",
    "check": "tsc --noEmit",
    "deploy": "pnpm dlx wrangler deploy --route \"${CF_WORKER_ROUTE}\"",
    "dev": "pnpm dlx wrangler dev",
    "d1:migrate": "pnpm dlx wrangler d1 migrations apply DRECLAW_DB",
    "test": "vitest run"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^4.20260215.0",
    "typescript": "^5.9.3",
    "vitest": "^3.2.4"
  },
  "dependencies": {
    "just-bash": "^2.10.2"
  }
}
